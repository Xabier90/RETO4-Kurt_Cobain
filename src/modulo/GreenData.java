package modulo;

import javax.swing.*;

public class GreenData extends JFrame {
	public GreenData() {
        setTitle("GreenData Developer");
        setSize(800, 600);
        setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
        add(new JLabel("Bienvenido a la gestión de base de datos"));
        setVisible(true);
    }
}
